{
  "name": "wasmphp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --target=esnext --outdir=dist --loader:.wasm=file --loader:.dat=file"
  },
  "dependencies": {
    "@php-wasm/web": "^2.0.11"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
