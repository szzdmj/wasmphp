{
  "name": "wasmphp",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/worker.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --target=esnext --outdir=dist --loader:.wasm=file"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
